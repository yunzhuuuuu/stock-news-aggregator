# Mini Robinhood 项目进度

最后更新：2026-09-20

本文件用于查看总体路线和完整更新历史。技术设计、数据结构与验收标准详见 [PLAN.md](PLAN.md)。每条更新简讯对应下方同编号的详情，方便先浏览再追溯。

## 总体计划

- **阶段 A：页面框架｜已完成**
  - 已完成：Next.js 初始项目、Dashboard、响应式布局、演示数据及页面内交互。
  - 阶段 A 当时限制：持仓只保存在当前页面；数据库持久化现已由阶段 B 代码取代，真实价格和新闻仍留待后续阶段。
  - 完成标准：桌面与手机都可操作，所有示例值均标为演示数据。
- **阶段 B：用户与持仓｜已完成**
  - 已完成：Supabase 登录/注册流程、positions 迁移、RLS、持仓增删改、独立注册页和本地环境连接。
  - 已验证：两个真实用户完成注册和登录，互相看不到对方持仓；退出后重新登录仍能看到各自之前保存的内容。
  - 完成标准：两个用户及未登录访问的隔离验证通过。
- **阶段 C：价格与计算｜已完成**
  - 已完成：daily_prices hosted 迁移、精确盈亏和 SMA 规则、Alpha Vantage 适配器、共享缓存写入、受保护的刷新接口、失败回退和 AAPL 真实价格验证。
  - 已验证：AAPL 真实计算、BBPL 无效代码提示和 NVDA 即时价格均在 Dashboard 正常。
  - 部署待办：阶段 E 再启用每天一次的定时触发器。
  - 完成标准：小数股数、零成本、休市日、缺价和数据不足都有正确结果或提示。
- **阶段 D：新闻｜已完成**
  - D1 已完成；D2 已实现五分钟缓存、原子租约、每日 90 次内部上限、旧缓存回退和本人持仓权限接口。
  - 修复迁移、函数权限、AAPL 真实新闻、五分钟缓存和每日计数均已通过验收；Dashboard、无效代码、标签切换、双账户和手机布局通过用户现场检查。
  - 完成标准：并发访问不重复请求供应商，接口故障时仍可查看旧新闻。
- **阶段 E：发布｜E1 已完成**
  - 本地 Git 与 Vercel 每日价格 Cron 已准备完成；下一步建立远程仓库并部署 Preview，再配置生产回调与完整验收。
  - 项目确认为个人、非商业用途；若最终允许任何人注册，再确认供应商对公开多用户展示与缓存的范围，或限制为个人演示访问。
  - 完成标准：登录、持仓、价格、新闻、手机页面和用户隔离在生产网站通过检查。

第一版目标：用户注册后管理自己的美股持仓，查看标明交易日期的日收盘价、未实现盈亏、规则信号和相关新闻。新闻在使用页面期间至多每五分钟检查一次；股价每个交易日收盘后更新一次。

## 更新索引

- **01｜环境与规划：确认 WSL 项目并写入技术方案**（2026-09-17）
  - 验证 main.py 可在 WSL 运行；建立 PLAN.md，明确网站范围、技术结构和实施阶段。详见[更新 01](#更新-01环境与规划)。
- **02｜脚手架：建立 Next.js 项目**（2026-09-17）
  - 生成 web/ 并安装依赖；阶段 A 进入进行中。详见[更新 02](#更新-02脚手架)。
- **03｜进度文档：建立、改版并合并记录**（2026-09-17）
  - 建立独立进度文档，改为分层列表，整理完整历史，并将原来的五条记录合并为现在的三条。详见[更新 03](#更新-03进度文档)。

- **04｜阶段 A：完成演示 Dashboard**（2026-09-17）
  - 建立响应式页面、演示数据与页面内持仓交互；桌面和手机布局、添加/删除及新闻切换通过检查。详见[更新 04](#更新-04阶段-a)。
- **05｜阶段 B：实现账户与持仓持久化**（2026-09-19）
  - 加入 Supabase Auth、positions 数据库迁移、RLS 和持仓增删改；代码检查通过，等待连接项目后验证真实用户隔离。详见[更新 05](#更新-05阶段-b)。
- **06｜阶段 B：连接 Supabase 并加固确认回调**（2026-09-19）
  - 将本地配置放入实际 Next.js 应用，兼容两种邮件确认格式，并完成连接、构建和安全失败路径检查。详见[更新 06](#更新-06阶段-b连接验证)。
- **07｜项目整理与认证体验完善**（2026-09-19）
  - 整理外层重复配置，修复浏览器扩展造成的 hydration 警告，并将登录与注册拆分为清楚的独立页面。详见[更新 07](#更新-07项目整理与认证体验完善)。
- **08｜阶段 B：完成双用户隔离与持久化验证**（2026-09-19）
  - 邮件额度恢复后第二个用户注册成功；两个用户的数据隔离、退出和重新登录后的持久化均通过实际检查。详见[更新 08](#更新-08阶段-b验收)。
- **09｜阶段 C：建立价格缓存与精确计算基础**（2026-09-19）
  - 新增 daily_prices 迁移、精确十进制盈亏与 SMA 信号计算、10 个边界测试，并完成 hosted Supabase 迁移与 Dashboard 空缓存验证。详见[更新 09](#更新-09阶段-c计算基础)。
- **10｜阶段 C：接入 Alpha Vantage 服务端刷新流程**（2026-09-20）
  - 新增可替换的价格供应商层、共享缓存刷新和受保护接口；16 个测试、lint、生产构建及未授权检查通过。详见[更新 10](#更新-10阶段-c价格供应商接入)。
- **11–12｜阶段 C：真实价格、无效代码与即时刷新**（2026-09-20）
  - AAPL 写入 100 个真实交易日，BBPL 被正确识别为无效代码；新增或编辑持仓后按需即时刷新，NVDA 验收通过。详见[更新 11–12](#更新-1112阶段-c-真实价格无效代码与即时刷新)。
- **13｜阶段 C 最终验收与 Stage D 规划**（2026-09-20）
  - NVDA 页面验收通过，Stage C 正式完成；完成 Marketaux、五分钟缓存、并发租约和额度保护的 Stage D 草案。详见[更新 13](#更新-13阶段-c收尾与-stage-d-规划)。
- **14｜Stage D1：建立新闻缓存与供应商适配器**（2026-09-20）
  - 新增四张新闻相关表的迁移和 Marketaux 适配器；28 个测试、lint 和构建通过，等待 hosted migration。详见[更新 14](#更新-14stage-d1-新闻基础)。
- **15–16｜Stage D2：新闻缓存、额度保护与函数修复**（2026-09-20）
  - 实现原子租约、五分钟缓存、每日 90 次上限、旧缓存回退和本人持仓接口；修复真实验收发现的 SQL 字段歧义。详见[更新 15–16](#更新-1516stage-d2-缓存额度保护与函数修复)。
- **17｜Stage D2：完成真实新闻与缓存验收**（2026-09-20）
  - 修复迁移执行后，AAPL 成功缓存 3 篇新闻；第二次请求命中缓存且每日计数保持为 1，D2 正式完成。详见[更新 17](#更新-17stage-d2-真实验收)。
- **18｜Stage D3：接入 Dashboard 新闻界面**（2026-09-20）
  - News 标签改为按需加载真实缓存新闻，加入来源、时间、摘要、原文链接及完整状态显示；37 个测试、lint 和构建通过。详见[更新 18](#更新-18stage-d3-dashboard-新闻界面)。
- **19｜Stage D：完成页面验收并规划 Stage E**（2026-09-20）
  - 用户确认新闻、无效代码、标签切换、双账户和手机布局均正常；补齐可见页面每五分钟检查并完成 Stage E 四步计划。详见[更新 19](#更新-19stage-d-验收与-stage-e-规划)。
- **20｜Stage E1：完成 Git 与 Vercel 本地准备**（2026-09-20）
  - 初始化本地 `main` 分支，确认密钥与生成目录被忽略；加入每日价格 Cron、生产变量清单并通过完整检查。详见[更新 20](#更新-20stage-e1-git-与-vercel-准备)。

## 更新详情

### 更新 01：环境与规划

- **改动**
  - 在项目根目录的 main.py 中，将打印内容设为 hello codex，以确认 WSL 文件夹可访问、可写入。
  - 创建 PLAN.md，定义第一版功能和边界：独立账户、美股持仓、每日收盘价、相关新闻与公开规则的信号。
  - 在计划中设计 Next.js、Supabase 和 Vercel 的分工，以及持仓、价格、新闻和刷新状态的数据表；划分 A 至 E 阶段并写出完成标准。
- **原因**：先确认开发环境可用，再明确产品范围与数据流程。
- **验证**：同项目先前任务记录显示，main.py 在 WSL 中输出 hello codex，PLAN.md 已写入。本任务重新读取过计划全文；本次没有重新运行 main.py。
- **当时状态**：网站项目尚未搭建，技术计划已完成。

### 更新 02：脚手架

- **改动**：在项目根目录生成 web/，包含 Next.js、React、TypeScript、Tailwind CSS、ESLint 和 npm 依赖；main.py 保留。
- **原因**：为阶段 A 提供可开发的网页项目结构。
- **验证**：先前任务的创建命令报告成功；随后确认 web/、package.json、默认 page.tsx、layout.tsx 和 globals.css 存在，并读取了它们。尚未运行网页或应用检查命令。
- **当前影响**：阶段 A 已开始，但 Dashboard、演示数据和响应式业务页面还没有写。

### 更新 03：进度文档

- **改动**
  - 最初建立 UPDATE_TRACKING.md，从 PLAN.md 摘出五阶段路线、当前进度和下一步计划，曾短暂在 PLAN.md 中加入链接；现已按用户要求移除。
  - 按用户反馈将文档改为分层 bullet points，用“更新索引 + 同编号详情”取代表格和分开的当前进度记录；当时文件曾命名为 PROGESS.md。
  - 当前项目中的文件名为 PROGRESS.md。本次把“环境 + 规划”和“建立追踪 + 改版 + 本次整理”分别合并，索引和详情由五条调整为三条；之后按用户要求让 PLAN.md 保持独立，移除了进度文档链接。
- **原因**：让总体路线、已完成工作和验证依据更容易阅读，同时避免同一次工作的文档调整分散成多条记录。
- **验证**：确认现有 PROGRESS.md 和 PLAN.md 的内容；本次修改后核对当时的三条索引与对应详情、文件名，并确认 PLAN.md 已恢复独立。没有运行应用检查，因为没有改应用代码。
- **目前状态**：阶段 A 的初始项目已生成；Dashboard 和后续阶段未开发。当时用户要求暂时不要写代码；之后已明确授权阶段 A。

### 更新 04：阶段 A

- **改动**
  - web/src/lib/demo-data.ts：定义演示持仓和新闻的数据形状，所有价格与标题均为明确标注的示例。
  - web/src/components/dashboard.tsx：实现总览、持仓选择、页面内添加与删除、Overview/News 切换及空状态。关键数据流和阶段边界写有面向初学者的注释。
  - web/src/app/page.tsx、layout.tsx、globals.css：用 Dashboard 替换生成器默认页面，设置页面标题和响应式样式。
- **原因**：先完成可操作的页面框架，下一阶段再把本地数组换成登录用户的数据库数据。
- **验证**
  - web/ 中的 npm run lint 与 npm run build 均通过。
  - 浏览器桌面截图已检查；在真实 390px 手机视口中，页面宽度与内容宽度均为 390px，没有横向溢出。
  - 浏览器交互检查通过：News 标签显示示例标题；添加 TEST 后持仓数从 3 变 4，价格显示 Not available；删除后数量回到 3。
- **阶段状态**：阶段 A 已完成。页面内容仍是演示数据，刷新后新增持仓会消失；登录与持久化属于阶段 B，价格、盈亏和信号属于阶段 C，真实新闻属于阶段 D。

### 更新 05：阶段 B

- **改动**
  - 安装 `@supabase/supabase-js` 和 `@supabase/ssr`，增加浏览器、服务端和 Next.js 16 Proxy 的 Supabase 客户端。
  - 增加登录、注册、退出和邮件确认流程；密码由 Supabase Auth 处理。
  - 增加 `positions` 数据库迁移：字段约束、唯一持仓约束、更新时间触发器，以及 SELECT、INSERT、UPDATE、DELETE 四条 RLS 策略。
  - 将 Dashboard 从浏览器内存数据改为登录用户的持久化持仓，并实现添加、编辑、删除、服务端校验和空状态。
  - 增加 `.env.example`、未配置时的引导页面，以及面向初学者的 `web/README.md` 连接和验证步骤。
- **原因**：让每位用户拥有独立账户，并将持仓可靠地保存到数据库；RLS 负责在数据库层阻止跨用户访问。
- **验证**
  - `npm run lint` 与 `npm run build` 通过；Next.js 正确生成首页、邮件确认 Route Handler 和 Proxy。
  - 已在浏览器检查未配置 Supabase 时的 setup 页面，页面能正常加载并显示完整连接步骤。
  - 当前环境没有 `.env.local`；Docker API 也不可访问，因此尚未运行真实注册、数据库 CRUD 和用户 A/B/未登录隔离验证。
- **阶段状态**：代码实现完成，等待连接一个 Supabase 项目并执行迁移。完成真实隔离验证后才能把阶段 B 标为“已完成”。

### 更新 06：阶段 B 连接验证

- **改动**
  - 将已有的 Supabase 项目 URL 和 publishable key 安全复制到 `web/.env.local`，让从 `web/` 启动的 Next.js 能实际读取配置；检查时没有输出变量值。
  - 更新 `web/src/app/auth/confirm/route.ts`，同时支持 `token_hash + type` 和 PKCE `code` 两种确认返回格式，并在跳转前清除一次性认证参数。
  - 更新 `web/README.md`，记录当前本地重定向通配符和 Supabase SSR 邮件模板设置。
- **原因**：原来的环境文件位于外层目录，实际应用无法加载；原确认路由只支持一种邮件确认格式。
- **验证**
  - `npm run lint` 通过。
  - `npm run build` 通过；Next.js 16.3.5 明确加载 `web/.env.local`，并生成动态首页、`/auth/confirm` 和 Proxy。
  - 本地主页返回 HTTP 200，显示登录和创建账户界面，没有显示未配置提示。
  - 分别使用无效的 PKCE code 和无效 token hash 检查确认路由；两者都返回到不含令牌的错误提示地址。
- **仍未验证**
  - 尚未用可接收邮件的真实测试账户完成注册、成功确认、登录和退出。
  - 尚未实际验证持仓增删改、刷新后持久化，以及用户 A、用户 B 和未登录访问的数据隔离。
  - 构建提示外层与 `web/` 同时存在 package lock；目前没有删除任何用户文件，这个警告不影响本次构建通过。
- **阶段状态**：Supabase 已连接，代码和安全失败路径通过检查；阶段 B 仍需真实账户端到端验证后才能标为完成。

### 更新 07：项目整理与认证体验完善

- **项目整理**
  - 新增根目录 `.gitignore`，忽略环境文件、依赖、Next.js 构建输出和日志。
  - 确认内外层环境配置一致后，将外层误生成的 `.env.local`、`package.json`、`package-lock.json` 和 `node_modules/` 移入临时备份。
  - 保留根目录的 `PLAN.md`、`PROGRESS.md`、`main.py` 和完整 `web/` 应用；之后所有 npm 命令都从 `web/` 运行。
  - 多 lockfile 和工作区根目录推断警告消失。
- **认证体验修复**
  - 处理 Night Eye 在 React 加载前修改 `<html>` 属性导致的 hydration 警告。
  - 将原来混在同一表单中的登录和注册流程整理为单一明确的认证 Action。
  - 登录页只负责登录；Create account 前往独立 `/signup` 页面，注册页单独收集邮箱、密码和确认密码。
  - 注册成功后显示 Check your email 状态；已登录用户访问 `/signup` 时返回 Dashboard。
  - 将 Supabase 的邮件额度错误改为容易理解的测试邮件提示，并在 README 记录限制。
  - 将意外出现且只含生成文件的 `web/web/.next` 移入现有临时备份，并让 ESLint 忽略任意层级的 `.next` 输出。
- **验证**
  - 登录页与注册页均返回 HTTP 200；构建生成动态 `/signup` 路由。
  - 第一个真实邮箱完成注册；第二个邮箱最初受到 Supabase 每小时测试邮件额度限制，额度恢复后在下一次验收中成功。
  - `npm run lint` 和 `npm run build` 均通过；修复后开发日志没有新增 hydration 或运行时错误。
- **备份清理**：用户确认非必要备份可以删除；此前保存的外层重复配置、依赖和嵌套构建输出已确认不再需要并清理，实际 `web/` 应用不受影响。
- **阶段状态**：项目目录和认证界面整理完成，等待双用户隔离验收。

### 更新 08：阶段 B 验收

- **验证结果（用户实际测试）**
  - Supabase 邮件额度恢复后，第二个真实邮箱完成注册。
  - 用户 A 和用户 B 登录后互相看不到对方的持仓内容。
  - 两个用户退出后重新登录，仍能看到自己之前保存的持仓。
  - 新的独立 `/signup` 注册流程显示和操作正常。
- **完成标准对应**
  - 两个用户的数据隔离通过，证明页面查询与 positions RLS 按用户生效。
  - 退出和重新登录后的数据仍存在，证明持仓已持久化到 Supabase，而不是只存在浏览器内存。
  - 未登录状态只显示认证界面，不能进入持仓 Dashboard。
- **阶段状态**：阶段 B 已完成。下一阶段为每日收盘价、盈亏计算和 SMA5/SMA20 规则信号。

### 更新 09：阶段 C 计算基础

- **改动**
  - 新增 `daily_prices` 迁移：按股票代码和交易日期保存共享日收盘价、币种、供应商和抓取时间；登录用户只能读取，浏览器不能写入。
  - 使用 `decimal.js` 完成持仓市值、成本、未实现盈亏、盈亏率、SMA5、SMA20 和规则信号的精确十进制计算。
  - 增加每个股票最近 20 个交易日的服务端读取层；多只股票分别限制 20 行，避免一个股票占满全局限制。
  - Server Component 在数据到达 Dashboard 前完成计算；Dashboard 展示总市值、总未实现盈亏、最新收盘价日期、单个持仓计算和 BUY/HOLD/SELL 或数据不足状态。
  - 迁移缺失时保留持仓功能并显示设置提示；迁移存在但无价格时显示 No prices，不使用假数据。
  - 新增测试命令；最终采用 Node 内置测试框架和 `tsx`，移除了带已知开发依赖问题的 Vitest。
- **测试覆盖**
  - 小数股数和示例盈亏：2.5 股、成本 100、收盘价 110 得到 25 美元未实现盈利。
  - 平均成本为零、完全缺价、最后可用交易日、少于 20 天历史。
  - BUY、HOLD、SELL 和严格正负 2% 边界。
  - 组合汇总只计算有价格的持仓，不把缺价持仓当作零价值。
- **验证**
  - `npm test`：10 个测试全部通过。
  - `npm run lint` 与 `npm run build` 通过。
  - `npm audit`：生产和开发依赖均为 0 个已知漏洞。
  - 2026-09-20 在 hosted Supabase 成功执行 `daily_prices` 迁移；刷新 Dashboard 后，原有价格缓存未准备提示消失。
  - 已登录账户的 AAPL 持仓仍正常显示为 2.5 股、平均成本 100 美元，证明新增迁移没有破坏现有持仓读取。
  - 空价格缓存状态符合预期：组合市值和总未实现盈亏显示 `No prices`；最新收盘价、市场价值和未实现盈亏显示 `Not available`；信号显示 `Signal: insufficient data` 和 `0 of 20 daily closes are available`。
- **尚未完成**
  - 尚未选择或连接真实价格供应商，也没有每日服务端抓取任务；当前不会显示真实价格。
- **阶段状态**：阶段 C 计算基础和 hosted 数据库迁移已完成并验证，但整个阶段未完成。下一步单独确认价格供应商的额度、历史能力和公开展示许可。

### 更新 10：阶段 C 价格供应商接入

- **供应商决定**
  - 开发阶段采用 Alpha Vantage 的日线接口；一次请求取得一只股票最近约 100 个交易日，满足 SMA20 所需历史长度。
  - 通过统一供应商接口隔离外部响应格式，以后可以更换供应商而不改 Dashboard 和数据库结构。
  - 免费开发额度适合当前小规模教学测试，但不视为公开多用户网站的展示许可；正式发布前仍需取得适当授权或更换方案。
- **改动**
  - 新增 Alpha Vantage 日线解析器：验证股票代码、日期和正数收盘价，识别错误与额度提示，并统一转换成 `daily_prices` 写入格式。
  - 新增服务端 Supabase admin client；`service_role` key 没有 `NEXT_PUBLIC_` 前缀，不发送给浏览器。
  - 新增共享刷新流程：读取全部持仓代码、去重、按顺序请求并 upsert 到缓存；单只股票失败不会删除旧数据，也不会阻止其他股票刷新。
  - 新增 `/api/prices/refresh` Route Handler；GET 和 POST 都要求 Bearer secret，并使用常量时间比较检查密钥。GET 为阶段 E 的每日定时触发器预留。
  - `.env.example` 和中文 README 已加入三个服务端变量、密钥安全说明和第一次真实刷新步骤。
- **测试覆盖**
  - 正常日线解析、日期排序、小数格式标准化、坏数据过滤、额度错误和空响应。
  - 股票代码去重、成功数据写入、单只股票失败后继续，以及失败时不写入坏数据。
- **验证**
  - `npm test`：16 个测试全部通过。
  - `npm run lint` 与 `npm run build` 通过；生产构建生成动态 `/api/prices/refresh` 路由。
  - 本地无 Authorization 请求实测返回 HTTP 401 和 `Unauthorized`，证明刷新接口默认不公开执行。
  - 检查 `.env.local` 时只列出变量名，没有输出任何密钥值；目前三个服务端变量尚未配置，因此未调用真实 Alpha Vantage，也未写入真实价格。
- **阶段状态**：Stage C 的供应商和刷新代码已完成。配置三个服务端变量并通过一次真实刷新与 Dashboard 检查后，才能完成本地阶段 C 验收；每日自动触发器将在阶段 E 部署时启用。

### 更新 11–12：阶段 C 真实价格、无效代码与即时刷新

- **真实刷新与无效代码**
  - 用户安全配置服务端变量后，AAPL 成功写入 100 个交易日；数据库核对的最新交易日为 2026-09-18、收盘价 336.13 美元。
  - BBPL 首次因请求过快触发供应商限制；加入不同股票间 1.2 秒等待后，供应商明确返回 `Invalid API call`，确认其为无效测试代码而非临时限流。
  - 新增错误分类和 `price_refresh_status`，分别记录 `ok`、`invalid_symbol` 与 `temporary_error`；失败不会删除旧价格，浏览器只能读取共享状态。
  - Dashboard 对无效代码显示 `Invalid stock symbol`，不再将其表现为普通的数据不足。
- **新增或编辑后的即时刷新**
  - 用户在另一个账户新增 NVDA 后，持仓虽已保存但没有立即取得价格，因此将持仓保存和价格刷新接到统一服务端模块。
  - 新增或编辑持仓后检查共享状态；12 小时内已有结果时复用缓存，否则只刷新该代码。供应商失败不会回滚已保存持仓。
  - NVDA 成功写入 100 个交易日，状态为 `ok`，最新缓存为 2026-09-18 的 222.27 美元。
- **验证与验收**
  - hosted `price_refresh_status` 迁移、字段与 RLS policy 通过检查；AAPL 为 `ok`，BBPL 为 `invalid_symbol`。
  - `npm test` 最终为 21 个测试，覆盖无效代码、限流、失败继续、请求间隔与 12 小时缓存边界；lint 和生产构建通过。
  - 用户确认 AAPL 计算、BBPL 红色提示，以及第二个账户的 NVDA 价格、盈亏和 SMA 信号均正常。
- **阶段状态**：AAPL、BBPL 和 NVDA 的页面验收全部通过，Stage C 已完成。

### 更新 13：阶段 C 收尾与 Stage D 规划

- **Stage C 最终结论**
  - 用户确认 AAPL 的真实价格与计算、BBPL 的无效代码提示、NVDA 的即时价格与计算全部正常。
  - Stage C 本地功能完成；每天自动更新价格的部署触发器按原计划留到 Stage E 配置。
- **Stage D 供应商建议**
  - 建议开发阶段使用 Marketaux：当前免费层每天 100 次、每次最多 3 篇，支持按股票代码、语言和发布时间过滤，比共用 Alpha Vantage 每天 25 次额度更适合新闻测试。
  - Marketaux 适合当前个人、非商业的教学开发；若 Stage E 保持公开自由注册，再核对公开多用户展示与缓存范围。
- **Stage D 三步方案**
  - D1：建立文章、股票关联、刷新状态和每日计数表；实现服务端 Marketaux 适配器与响应校验测试。
  - D2：实现仅允许查询本人持仓代码的新闻接口、五分钟共享缓存、数据库原子租约、每日 90 次内部上限和故障时旧缓存回退。
  - D3：连接 Dashboard News 标签；只在标签可见时检查，展示来源、发布时间、抓取时间、摘要和原文链接，并完成覆盖、去重、并发和故障实测。
- **当前状态**：只完成规划，没有写 Stage D 应用代码、创建新闻迁移或调用新闻供应商。等待用户批准方案。

### 更新 14：Stage D1 新闻基础

- **数据库迁移**
  - 新增 `articles`：只保存供应商文章 ID、标题、来源、HTTPS 原文链接、发布时间、抓取时间和最多 500 字符的短摘要，不保存全文。
  - 新增 `article_symbols`：让同一篇文章可以关联多个股票代码，并用联合主键去重。
  - 新增 `news_refresh_state`：为 D2 的五分钟缓存、短租约、成功时间和错误回退保留状态。
  - 新增 `news_api_usage`：按日期记录服务端请求次数，为每天 90 次内部上限提供依据。
  - 四张表全部启用 RLS；登录用户只能读取文章、关联和刷新状态，浏览器不能写入，额度计数仅服务端可见。
- **Marketaux 适配器**
  - API token 只读取服务端 `MARKETAUX_API_TOKEN`，不使用 `NEXT_PUBLIC_`。
  - 统一校验文章 ID、标题、HTTPS 链接、发布时间和股票关联；移除 URL 跟踪参数，过滤无关股票、HTTP 链接、坏日期和重复 ID/URL。
  - 只保留供应商提供的短摘要并限制为 500 字符；合法空结果返回空列表，不编造新闻。
  - 明确分类 HTTP 429 和其他供应商错误，为 D2 的旧缓存回退做准备。
- **验证**
  - `npm test`：28 个测试全部通过，其中 7 个覆盖 Marketaux 正常解析、坏数据过滤、去重、空结果、错误、摘要长度、查询参数和限流。
  - `npm run lint` 与 `npm run build` 通过。
  - 本阶段没有配置或调用 Marketaux token，没有消耗真实新闻 API 额度。
- **hosted migration 验收**
  - 用户截图确认 `articles`、`article_symbols`、`news_refresh_state` 和 `news_api_usage` 已建立。
  - 服务端只读检查确认四张表均可访问且当前为 0 行。
  - 使用 publishable key 的未登录读取被 PostgreSQL 以 `42501` 拒绝，证明匿名用户不能读取文章缓存。
- **当前状态**：D1 已完成。等待用户在本地安全配置 `MARKETAUX_API_TOKEN`，然后开始 D2；没有调用真实 Marketaux 或消耗新闻额度。

### 更新 15–16：Stage D2 缓存、额度保护与函数修复

- **数据库并发控制**
  - 新增 `claim_news_refresh`：在数据库事务内锁定单个股票状态，依次检查五分钟缓存、已有 30 秒租约和当天预算；只有获得租约的请求才原子增加 `news_api_usage`。
  - 新增 `complete_news_refresh`：成功后缓存五分钟并记录成功时间；失败后清除租约、保留旧文章，并等待一分钟再重试。
  - 两个函数都撤销匿名与登录用户执行权，只授予 `service_role`。
- **服务端缓存与接口**
  - 新增供应商无关的新闻刷新流程：先读缓存，再申请租约；成功则保存并重新读取，失败、并发刷新或预算耗尽时返回已有缓存。
  - 新增 `/api/news?symbol=...`；必须登录，并且股票代码必须存在于当前用户自己的 RLS 持仓中，防止把网站变成可消耗额度的任意代码代理。
  - 新闻接口不向浏览器暴露 Marketaux token、全站每日请求计数或数据库内部错误。
- **首次真实验收发现的问题**
  - 用户同意只发送 AAPL 做首次真实新闻验收后，服务端在申请数据库刷新租约时收到 `column reference "request_count" is ambiguous`。
  - 错误发生在 Marketaux 请求之前，因此没有向供应商发送 AAPL，也没有消耗 Marketaux 额度。
- **修复**
  - 将 `claim_news_refresh` 的公开返回字段从 `request_count` 改为 `usage_count`，避免与 `news_api_usage.request_count` 重名。
  - 为所有状态和用量表字段加入明确别名，并同步修改服务端结果映射。
  - 新增 `202609200004_fix_news_refresh_claim.sql`；已执行过原始迁移的 hosted 项目只需运行这一份修复迁移。
  - 同步修正原始 D2 迁移，确保以后从零建立数据库时不会带入旧问题。
- **验证**
  - `npm test`：33 个测试全部通过，包括缓存命中、成功刷新、供应商失败、并发租约和预算耗尽。
  - `npm run lint` 与 `npm run build` 通过；构建生成动态 `/api/news` 路由。
  - 未登录调用新闻接口返回 HTTP 401；数据库匿名函数调用被 `42501` 拒绝。
- **后续状态**：用户已执行 `202609200004_fix_news_refresh_claim.sql`，最终验收结果见更新 17。

### 更新 17：Stage D2 真实验收

- **权限验证**
  - 修复后的服务端函数可以正常执行参数验证。
  - 使用 publishable key 的未登录调用继续被 PostgreSQL 以 `42501` 拒绝。
- **真实新闻与缓存**
  - 第一次对 AAPL 刷新成功，Marketaux 返回并缓存 3 篇有效文章；当天内部请求计数从 0 增加到 1。
  - 紧接着第二次刷新返回 `fresh`，读取同样 3 篇缓存文章，没有再次调用供应商；当天计数仍为 1。
  - 刷新状态记录成功时间和五分钟后的下次允许时间；租约已清除，`last_error` 为空。
- **阶段状态**：Stage D2 已完成。下一步是 D3，将真实新闻接入 Dashboard News 标签，并完成多用户权限、状态显示和手机布局验收。

### 更新 18：Stage D3 Dashboard 新闻界面

- **按需加载**
  - 只有用户打开某个持仓的 News 标签时，浏览器才请求 `/api/news`；Overview 和持仓列表不会调用新闻接口。
  - 切换股票时自动回到 Overview，避免用户没有主动查看新闻时消耗共享额度。
  - 已由价格供应商确认无效的代码只显示不可用提示，不触发新闻请求。
- **新闻展示与状态**
  - 每篇文章展示来源、发布时间、标题、供应商短摘要、缓存时间和 HTTPS 原文链接；链接在新标签打开。
  - 加入加载中、刚刷新、命中缓存、另一请求正在更新、每日预算耗尽、供应商失败回退和合法空结果提示。
  - 浏览器对接口返回值再次检查文章字段、日期、状态和 HTTPS 链接；异常数据会变为清楚的错误提示。
  - 新闻卡片沿用现有 Dashboard 视觉样式，并为窄屏调整间距和元数据排列。
- **验证**
  - 新增 4 个客户端响应校验测试；`npm test` 共 37 个测试全部通过。
  - `npm run lint` 和 `npm run build` 通过，动态 `/api/news` 路由正常生成。
- **后续状态**：用户已完成浏览器现场验收，最终结果见更新 19。

### 更新 19：Stage D 验收与 Stage E 规划

- **Stage D 最终验收**
  - 用户确认 AAPL 新闻内容与链接、Overview/News 标签切换、BBPL 无效代码提示、另一个账户的持仓新闻和手机宽度布局均正常。
  - 对照 PLAN 时补齐 News 标签保持打开且页面可见时每五分钟自动检查；离开标签时自动停止，且同一组件不会发出重叠请求。
  - 补充后 `npm test` 的 37 个测试、`npm run lint` 和 `npm run build` 再次全部通过。
  - Stage D 正式完成。
- **Stage E 规划**
  - E1：安全建立 Git 基础、Vercel 配置、每日价格 Cron 和生产环境变量清单。
  - E2：从 `web/` 部署 Preview，验证构建、受保护接口和日志。
  - E3：设置 Supabase 的生产 Site URL 与精确回调地址；在公开启用真实新闻和价格前解决供应商公开展示与缓存许可。
  - E4：发布 Production，用两个账户完成全流程和手机验收，并记录最终检查结果。
- **项目用途与许可**：用户确认项目为个人、非商业用途。E1/E2 可继续；如果 Production 仍允许任何人注册，需要再确认第三方数据公开多用户展示与缓存范围，或改为个人演示访问。
- **文档与备份整理**：按用户要求合并更新 11–12 和 15–16；外层重复配置、依赖和构建输出备份已确认不再需要并删除。
- **后续状态**：用户已批准 E1；完成结果见更新 20。

### 更新 20：Stage E1 Git 与 Vercel 准备

- **Git 安全基础**
  - 在项目根目录初始化本地 Git 仓库，默认分支为 `main`；尚未配置远程地址或上传任何文件。
  - Git 忽略检查确认 `web/.env.local`、`web/.next` 和 `web/node_modules` 不会被纳入版本控制。
- **Vercel 配置**
  - 新增 `web/vercel.json`，每天 23:00 UTC 请求一次受保护的 `/api/prices/refresh`；该时段全年晚于美股常规收盘。
  - 价格接口沿用现有常量时间密钥比较，并兼容 Vercel 自动发送的 `CRON_SECRET` Bearer token。
  - `.env.example` 与中文 README 增加 `CRON_SECRET` 和完整 Vercel 环境变量清单；只记录变量名与占位符，没有写入真实值。
- **验证**
  - `vercel.json` 通过 JSON 格式检查。
  - `npm test`：37 个测试全部通过；`npm run lint` 和 `npm run build` 通过。
  - `npm audit`：0 个已知漏洞。
- **当前状态**：E1 已完成。下一步 E2 需要用户选择远程仓库位置，然后推送代码并创建 Vercel Preview；仍未发布网站。

## 后续记录约定

- 新更新先在“更新索引”加一条简讯，再在“更新详情”加同编号的小节。
- 详情记录改动文件、原因、验证结果、已知限制和阶段状态变化。
- 只有实际完成并验证的工作才标为“已完成”；计划中的内容留在阶段待办里。
